#!/bin/sh

## This script uses https://www.npmjs.com/package/http-server NPM package
## to run a local, simple, and minimal enough HTTP Server.
## Use "npm i -g http-server" to install it globally.

http-server . -p 3000

